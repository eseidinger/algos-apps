from setuptools import setup

setup(
    name="py_algos",
    version="0.1.0",
    packages=["generic_search"],  # Add the name of your package
    install_requires=[],  # Add any dependencies
    author="Your Name",
    description="A brief description of your package",
)
